# ReManga API documentation

- The public repository contains generated API documentation and the portable generator source.
- Build the generator with `./generator/gradlew.bat -p generator build`.
- Regenerate documentation with `./generate_api_docs.ps1`, then review the resulting JSON, CSV, and Markdown diffs before committing.
- Do not copy private runner configuration, credentials, or unpublished endpoints into this repository.
